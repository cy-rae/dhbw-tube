apiVersion: v1
kind: Service
metadata:
  name: minio-service
  namespace: dhbw-tube
spec:
  ports:
    - name: admin-console
      port: 9000
      targetPort: 9000
    - name: gui
      port: 9001
      targetPort: 9001
  selector:
    app: minio-app